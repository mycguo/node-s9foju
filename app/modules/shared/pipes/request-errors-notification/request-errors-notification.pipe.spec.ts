import { RequestErrorsNotificationPipe } from './request-errors-notification.pipe';

describe('RequestErrorsNotificationPipe', () => {
  it('create an instance', () => {
    const pipe = new RequestErrorsNotificationPipe();
    expect(pipe).toBeTruthy();
  });
});
