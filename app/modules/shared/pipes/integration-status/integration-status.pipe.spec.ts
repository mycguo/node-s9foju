import {IntegrationStatusPipe} from './integration-status.pipe';

describe('IntegrationStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new IntegrationStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
