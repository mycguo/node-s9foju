export default interface OldSortableList {
  id: string;
  name: string;
  active?: boolean; // selected item
  enabled?: boolean; // item switcher enabled or isDisabled
  disabled?: boolean; // item isDisabled
}
