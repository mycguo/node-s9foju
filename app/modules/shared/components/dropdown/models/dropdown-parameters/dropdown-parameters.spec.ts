import { DropdownParameters } from './dropdown-parameters';

describe('DropdownParameters', () => {
  it('should create an instance', () => {
    expect(new DropdownParameters()).toBeTruthy();
  });
});
