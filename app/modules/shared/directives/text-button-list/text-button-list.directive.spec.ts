import { TextButtonListDirective } from './text-button-list.directive';

describe('TextButtonListDirective', () => {
  it('should create an instance', () => {
    const directive = new TextButtonListDirective();
    expect(directive).toBeTruthy();
  });
});
