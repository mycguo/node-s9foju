import { ChipListDirective } from './chip-list.directive';

describe('ChipListDirective', () => {
  it('should create an instance', () => {
    const directive = new ChipListDirective();
    expect(directive).toBeTruthy();
  });
});
