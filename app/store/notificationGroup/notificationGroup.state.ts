import NotificationGroup from './notificationGroup.model';
import {EntityState} from '@datorama/akita';

interface NotificationGroupState extends EntityState<NotificationGroup> { }

export default NotificationGroupState;
